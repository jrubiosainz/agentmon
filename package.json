{
  "name": "agentmon",
  "version": "1.0.0",
  "private": true,
  "description": "Agentmon - a GBA-style robot monster collecting RPG",
  "type": "module",
  "engines": { "node": ">=20" },
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "start": "node server/dist/index.js",
    "typecheck": "npm --workspace client run typecheck && npm --workspace server run typecheck",
    "test": "npm --workspace client run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
